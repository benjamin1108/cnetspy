# 主配置文件 - 精简版

imports:
  - crawler.yaml        # 爬虫配置
  - sources.yaml        # 数据源配置
  - logging.yaml        # 日志配置
  - ai_model.yaml       # AI 模型配置
  - notification.yaml   # 通知推送配置
  - scheduler.yaml      # 定时任务配置
